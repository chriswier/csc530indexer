root@k8s1:~# helm install incubator/solr --generate-name --set javaMem="-Xms2g -Xmx2g",livenessProbe.initialDelaySeconds=420,exporter.readinessProbe.periodSeconds=30,volumeClaimTemplates.storageSize=50Gi
NAME: solr-1602098385
LAST DEPLOYED: Wed Oct  7 15:19:45 2020
NAMESPACE: default
STATUS: deployed
REVISION: 1
TEST SUITE: None
NOTES:
Your Solr cluster has now been installed, and can be accessed in the following ways:

  * Internally, within the kubernetes cluster on:

solr-1602098385-svc.default:8983

  * External to the kubernetes cluster:

export POD_NAME=$(kubectl get pods --namespace default -l "app=solr,component=server,release=solr-1602098385" -o jsonpath="{ .items[0].metadata.name }")
echo "Visit http://127.0.0.1:8983 to access Solr"
kubectl port-forward --namespace default $POD_NAME 8983:8983


